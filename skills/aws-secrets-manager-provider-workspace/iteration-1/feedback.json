{
  "eval-normal-aspnet-setup": "Both conditions passed all 3 assertions, but the without_skill answer named an entirely wrong NuGet package (Kralizek.Extensions.Configuration.AWSSecretsManager, the upstream fork this package descends from) and set AcceptedSecretArns = null as a stray no-op. The assertions didn't catch either issue — worth adding an assertion like 'names the correct package AWSSecretsManager.Provider' before the next iteration.",
  "eval-advanced-batch-poll-keygen": "with_skill was clean. without_skill missed the BatchGetSecretValue IAM permission mention (assertion failure) and separately invented a non-existent options.BatchSize property, which the assertions didn't test for.",
  "eval-edge-json-flattening-casing": "Both conditions passed fully — this case tests generic Microsoft.Extensions.Configuration behavior (':'-delimited flattening, case-insensitivity) that a capable model gets right without the skill. Low discriminating power; consider replacing or sharpening this case in a future iteration (e.g. ask about array-of-object flattening or the malformed-JSON fallback instead, which are less commonly known).",
  "eval-misunderstood-ignoremissingvalues-batch": "Both conditions reached the correct conclusion. without_skill fabricated an inaccurate parallel-Task.WhenAll mechanism (the real implementation is a sequential foreach over chunks) and gave a code sample that wouldn't compile as shown (positional args would bind to `region`, not `configurator`). Neither is covered by current assertions; consider adding one that checks the example code is API-accurate.",
  "eval-value-add-localstack-logging": "Clear skill value: with_skill correctly used ConfigureSecretsManagerConfig/ServiceURL; without_skill invented client-injection overloads that don't exist on SecretsManagerExtensions, which would fail to compile if a user copied it verbatim.",
  "eval-edge-binary-secret-missing": "Clear skill value: with_skill led with the binary-secret explanation (the actual documented behavior); without_skill produced a longer, plausible-sounding list of 7 causes but never mentioned binary secrets at all, missing the one fact this eval exists to test."
}
