{
  "assertion_results": [
    {
      "text": "Sets UseBatchFetch to true and mentions the 20-secret batch chunk limit",
      "passed": true,
      "evidence": "Sets `options.UseBatchFetch = true` and states 'AWS caps BatchGetSecretValue at 20 secret IDs per request'."
    },
    {
      "text": "Mentions the secretsmanager:BatchGetSecretValue IAM permission is required",
      "passed": false,
      "evidence": "No mention anywhere of the secretsmanager:BatchGetSecretValue (or any) IAM permission requirement for batch fetch."
    },
    {
      "text": "Sets PollingInterval to enable background reload rather than suggesting a manual restart",
      "passed": true,
      "evidence": "Sets `options.PollingInterval = TimeSpan.FromMinutes(5)` and correctly explains IOptionsMonitor/change-token semantics."
    },
    {
      "text": "Sets KeyGenerator to uppercase the key rather than inventing an unrelated casing option",
      "passed": true,
      "evidence": "Sets `options.KeyGenerator = (secret, key) => key.ToUpperInvariant();`."
    }
  ],
  "summary": { "passed": 3, "failed": 1, "total": 4, "pass_rate": 0.75 },
  "notes": "Also invents a non-existent `options.BatchSize = 20` property not present on the real SecretsManagerConfigurationProviderOptions API, and hedges with 'double-check the exact member names... since these can vary slightly between forks/versions' — an explicit admission of uncertainty about the API surface that the skill would have resolved."
}
